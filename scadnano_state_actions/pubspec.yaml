name: scadnano_state_actions
description: State types, actions, and serializers for scadnano - extracted to a separate package to optimize built_value code generation caching.

environment:
  sdk: '>=3.7.0 <4.0.0'

resolution: workspace

dependencies:
  built_value: ^8.9.0
  built_collection: ^5.1.1
  collection: ^1.15.0-nullsafety.4
  color: ^3.0.0
  js: any
  platform_detect: ^2.1.0
  quiver: ^3.2.2
  spreadsheet_decoder: ^2.2.0

dev_dependencies:
  build_runner: ^2.14.1
  build_web_compilers: ^4.0.11
  built_value_generator: ^8.9.0
